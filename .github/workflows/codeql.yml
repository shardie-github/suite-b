name: codeql
on:
  push:
    branches: [ main ]
  pull_request:
  schedule:
    - cron: '23 2 * * 2'
jobs:
  analyze:
    uses: github/codeql-action/.github/workflows/codeql.yml@v3
    with:
      languages: javascript-typescript
