name: ci
on: { push: { branches: [main] }, pull_request: {} }
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: |
          npm --prefix packages/web i
          npm --prefix packages/slack i
          npm --prefix packages/stripe i
      - run: echo "✅ Suite B CI OK"
